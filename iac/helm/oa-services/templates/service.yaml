apiVersion: v1
kind: Service
metadata:
  name: {{ include "oa-services.fullname" . }}
  labels:
    {{- include "oa-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oa-services.selectorLabels" . | nindent 4 }}
