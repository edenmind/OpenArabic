apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oa-services.fullname" . }}-test-connection"
  labels:
    {{- include "oa-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oa-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
