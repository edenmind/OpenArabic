apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: static
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: static
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75