import { createAction } from '@reduxjs/toolkit'

export const SET_CATEGORIES = createAction('SET_CATEGORIES')
export const SET_TEXT = createAction('SET_TEXT')
export const SET_TEXTS = createAction('SET_TEXTS')
export const RESET_TEXT = createAction('RESET_TEXT')
export const SET_TEXT_LOADED = createAction('SET_TEXT_LOADED')
export const SET_TEXTS_LOADED = createAction('SET_TEXTS_LOADED')
