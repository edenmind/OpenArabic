import * as quiz from '../services/Quiz'
import { expect, describe, it } from '@jest/globals'

describe('quiz', () => {
  it('should truncate', () => {
    // Arrange

    // Act

    // Assert
    expect(true).toBe(true)
  })
})
